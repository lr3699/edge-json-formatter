# 更新日志

## 1.1.3 — 2026-09-20

### 变更 · 更名「JSON Duo」+ 中英双语 i18n

- **扩展名改为 `JSON Duo`**（原名「JSON 格式化查看器 - JSON Formatter Pro」）。
- **加 `_locales` i18n**：`default_locale: zh_CN`，manifest 的 `name`/`short_name`/
  `description`/`action.default_title` 改为 `__MSG_*__` 占位符，提供
  `_locales/zh_CN/messages.json` 与 `_locales/en/messages.json`。这让 Partner Center
  识别出「中文（简体）」主语言，商店名称与描述可走中文。
- **描述改为中文**：定位从「JSON 格式化查看器」改为「双栏 JSON 工作台」——左栏粘贴
  原文、右栏实时预览，突出内置编辑页与网页自动接管两大入口。
- 同步更名所有引用旧名的文件：README / PRIVACY / site(index+privacy) / popup / options /
  preview demo / tools/serve.js / tools/build.js（zip 文件名 `json-formatter-pro-` → `json-duo-`，
  打包日志、INCLUDE_DIRS 加 `_locales`）/ tools/verify-package.js。

### 变更 · 点击图标直接打开编辑页（去掉二级 popup）

- **点击工具栏图标不再弹 popup，直接打开「粘贴 JSON 格式化」编辑页**：移除
  `manifest.action.default_popup`，在 service worker 里监听 `chrome.action.onClicked`
  调 `NS.openEditor()`（自带去重 + 聚焦，点几次都只有一个编辑页标签）。
- 弹窗里原有的「格式化当前页面 / 还原原文」仍有替代入口：右键菜单
  「用 JSON 格式化查看当前页面」、快捷键 `Alt+Shift+J`、以及内容脚本自动接管。

### 修复 · 压缩按钮真正生效 + 深色主题全局化

- **「压缩」此前点了没反应**：旧逻辑切换 `outMode` 后只调 `updateStats()`，不重渲染，
  屏幕上的树纹丝不动，用户以为坏了。现在点「压缩」会 `render()`，把整棵树渲染成
  **单行扁平文本**（键+值+分隔符连续排列、不折叠、不换行、无缩进引导线），点「美化」
  恢复树形。叶子值保留语法高亮与点击复制；`renderCompactNode` / `renderCompactChild`
  递归拼接，16 万节点也只是一行 DOM，无需分批。
- **深色主题只作用右侧查看器** → 全局：viewer 新增 `onThemeChange` 回调，独立编辑页
  监听后把 `data-theme` 挂到 `<html>`；`editor.css` 的深色 token 改为
  `html[data-theme="dark"]` 覆盖（系统自动跟随仍用 `prefers-color-scheme`）。
  现在切深色，顶栏、输入面板、极光背景、状态栏全部一起变暗。
- 页面打开尚未粘贴内容时，也按已保存主题预铺底色（`NS.loadSettings` 回调里判断）。

### 变更 · 默认全展开 + 工具栏精简为 5 个按钮

- **格式化后默认展开全部节点**：不再只展开到「默认层级」，改由 `setAllExpanded(true)`
  全展开。渲染本身是分片的（`RENDER_BUDGET` / `MAX_CHUNK`），超大 JSON 仍然只先建
  可视范围内的一批，其余走「加载更多」，不会因为全展开而卡死。
- **移除搜索定位**：删掉 `performSearch` / `revealNode` / `jumpTo` 等整套匹配跳转逻辑、
  搜索框与命中计数 UI、以及 `.jf-search*` / `.jf-mark*` 样式。
- **工具栏只留 5 个功能**：保留转义、主题、压缩（美化/压缩切换）、复制、下载。
  折叠/展开全部、展开层级、缩进、还原原文、关闭全部移除（关闭统一收敛到 `Esc`）。
- **全部改成按钮**：新增 `.jf-btn-solid` 实心描边按钮样式，保留转义 / 主题 / 压缩
  由原来的 checkbox + select 改为可点击按钮，启用态用 `.jf-btn-on` 高亮（主题按钮
  按「跟随系统 → 浅色 → 深色」循环，压缩按钮在美化/压缩间切换并同步文案）。
- 设置页移除已失效的「默认展开层级」选项。

## 1.1.2 — 2026-09-20

修复：粘贴大 JSON（10MB 级）后 `Ctrl+V` 卡顿很久。真正根因不是解析（实测 154ms）
也不是渲染（8 行 <1s），而是**浏览器往 `<textarea>` 塞十几 MB 文本后，对超长文本
节点做布局/绘制会阻塞主线程数秒**——粘贴 11MB 实测从「派发 input」到「首行出现」
要 7~11 秒，且任何 JS 优化都绕不开这道 textarea 的物理瓶颈。

### 修复 · 大输入改虚拟滚动只读原文视图（`editor.js` / `editor.html` / `editor.css`）

- **大输入不进 textarea**：粘贴/拖入超过 512KB 的文本时拦截 `paste` 事件（挂在
  `document` 上，大输入模式下 textarea 隐藏后仍能再粘一份替换），文本只存内存
  变量 `sourceText`，绝不写回 `<textarea>`——那才是卡顿根因。
- **虚拟滚动原文视图**：原文按行拆成虚拟列表（`indexOf` 逐行建索引，O(行数)），
  只渲染可视区域、上下各留缓冲，带行号、可流畅滚动。20MB 内原文完整可见、逐行
  可查，不再用「占位提示」把正文藏起来。格式化结果照常进右侧树（已分批渲染 +
  行数上限，扛得住 16 万节点）。
- **小输入照常实时编辑**：≤512KB 的文本仍走 textarea 打字 + 220ms 防抖，行为不变。

### 修复 · 序列化移出首屏关键路径（`viewer.js`）

`updateStats()` 原来在 `setText` 同步链路上调用 `outputText()`（全树 `pretty` 序列化，
11MB 约 280ms），白白拖慢「粘贴 → 首屏」。现在只在缓存命中（用户点过复制/导出）
时才展示「输出体积」，序列化完全移出关键路径，按需再算。

### 调整 · 自动格式化上限 12MB → 20MB

`maxAutoSize` 默认 12MB → **20MB**：大输入已绕过 textarea 这个卡顿源，
20MB 内都能流畅自动格式化。设置页下拉同步补了 10MB / 20MB 两档。

### 实测对比（11MB 真实文件，FeHelper 导出）

| 指标 | 修复前 | 修复后 |
| --- | --- | --- |
| 粘贴动作本身 | 584ms 赋值 + 数秒 textarea 重排 | **约 20ms**（拦截，不写 textarea） |
| textarea 承载 11MB | 是（卡顿根因） | 否（虚拟滚动只渲染可视行） |
| 粘贴 → 首行出现 | 7~11 秒 | **约 1 秒** |

## 1.1.1 — 2026-09-20

修复：格式化大 JSON（10MB 级）直接卡死。四个根因全部处理，实测 12MB / 10 万项
数据从「无限假死」降到约 12 秒出结果（含 12MB 文本框赋值本身的开销，解析 0.6 秒），
且全程 DOM 行数有硬上限，折叠 / 展开 / 搜索都不再触发渲染爆炸。

### 修复 · 解析器两处 O(n²) 热点（`json-parser.js`）

- **字符串转义搜索扫全文**：`readString` 旧版先在**整个文档**上
  `indexOf('\\')` 找转义符——文档里只要没有任何反斜杠，每个字符串都要白扫到
  文档末尾，几十万个短字符串就是 O(n²)。改为先定位结束引号、再在「引号界定
  的一段」内找反斜杠。字符串数组场景 562ms → 15ms，单一大对象 1673ms → 32ms。
- **宽松模式裸键匹配复制剩余文本**：`relax` 旧版对每个裸键先 `text.slice(i)`
  再配正则，等于每个键都复制「当前位置到结尾」的整段文本。改为 sticky 正则
  直接在原文上 `exec`，空白扫描用逐字符判断。12MB 宽松输入的 `relax` 从
  「分钟级」降到 0.7 秒。

### 修复 · 查看器渲染爆炸与重复序列化（`viewer.js`）

- **分批渲染 + 渲染预算**：旧版 `render()` 会把展开到的所有节点一次性建完
  DOM（10 万项数组 ≈ 30 万行，页面直接假死）。现在每个容器单批最多 400 行、
  一次渲染动作总预算 5000 行；超出部分显示「… 还有 N 项，点击加载更多」，
  点击后继续分批加载。
- **`outputText()` 结果缓存**：旧版 `updateStats()` 每次渲染 / 折叠都把整棵树
  序列化成 10MB 级字符串，只为显示「输出多大」。现在按「模式 + 缩进 + 树版本」
  缓存，重复调用 O(1)。
- **搜索跳转窗口化**：命中行落在「加载更多」未渲染区域时，旧思路「补齐渲染
  到目标」在目标很远时会再次引爆 DOM。现在距离一个分块内就补齐；太远则
  从目标附近开窗重渲染（类似虚拟滚动），命中行照常高亮滚动到位。
- 展开全部等批量操作同样受渲染预算约束：实测 10 万项「展开全部」3 秒、
  6000 行封顶，不再卡死。

## 1.1.0 — 2026-09-20

两件事：补上「自己粘贴 JSON 来格式化」这条入口；修掉正文字太小太细的问题。
另附带一个全屏沉浸模式，并清掉了动效里会让窗口闪动的几处写法。

### 新增 · 粘贴 JSON 格式化（独立编辑页）

此前扩展只能格式化**网页上的** JSON。从聊天记录、服务端日志、终端里复制出来的 JSON 没有地方贴
——这是最常被问到的一个缺口。现在补上了：

- 新增 `src/editor/`（页 / 样式 / 逻辑），三栏布局：输入区 · 可拖动分隔条 · 格式化输出区。
- **输入**：`Ctrl+V` 直接粘、拖入 `.json` / `.txt` 文件、点「示例」灌一段样例数据；输入有 220ms 防抖。
- **输出**：完整复用网页端的查看器（折叠树、语法高亮、搜索、路径复制、导出），
  所以能力与接管网页时完全一致。
- **格式化动效**：内容变更后逐行错落浮现（每行延迟 11ms，最多 40 行，420ms 缓动），
  工具栏自上滑入、状态栏淡入。动效只在**真正的内容变更**时播放（粘贴 / 拖入 / 载入示例）；
  逐字打字不播，否则会一直闪。尊重 `prefers-reduced-motion`。
- **全屏沉浸模式**：顶栏的「全屏」按钮、快捷键 `F` 都能切换（`Esc` 退出）。
  进入后隐藏输入区与分隔条，格式化结果独占整个工作区，同时请求浏览器全屏。
  先切布局、再调 `requestFullscreen()`，所以即使页面嵌在 iframe 里没给 `allowfullscreen`
  导致全屏被拒，沉浸布局照常生效，观感一致。
- **超大内容保护**：粘贴体积超过「自动格式化上限」时只提示不渲染，按 `Ctrl+Enter` 可强制格式化。
- **三个入口**：工具栏弹窗顶部的「粘贴 JSON 格式化」按钮、右键菜单「粘贴 JSON 格式化（打开编辑页）」、
  设置页「快速上手」卡片里的按钮。同一时间只保留一个编辑页标签，重复点击会聚焦已有页面。
- 分隔条位置记在 `localStorage`，双击或按 `Home` 恢复默认宽度；键盘 `←/→` 可微调。
- 该页**不依赖 `chrome.*`**，所以也能脱离扩展直接用浏览器打开
  （`node tools/serve.js 18544` → `/src/editor/editor.html`，见 README「四种看效果的方式」）。
  脱离扩展时会自动隐藏顶栏的「设置」按钮，其余能力与扩展里完全一致。

> 实现上 `NS.openEditor()` 用 `chrome.runtime.getContexts()` 查找已打开的编辑页。
> 不用 `tabs.query({url})` 是因为：带 url 过滤器需要 `tabs` 权限（否则过滤器被静默忽略），
> 而本扩展只有 `host_permissions: <all_urls>`，覆盖不到 `chrome-extension://`。
> 为了不为一处体验多申请一条权限，改用不需额外权限的 `getContexts()`（Chrome/Edge 116+ 起可用）。

### 调整 · 正文字体可读性

- **默认字号 13 → 15 px**：13px 在 1080P 以上的屏幕上长时间阅读偏吃力。设置页字号选项同步扩到
  12 / 13 / 14 / 15 / 16 / 18 / 20。
- **正文 `font-weight` 400 → 500**：笔画更实，缩进层级更容易辨认。
- **移除 `-webkit-font-smoothing: antialiased`**：它在 Windows 上会把笔画渲染得更细，
  是「看起来发虚、发细」的主因。
- **字体栈前置 `Cascadia Mono` / `Consolas`**：此前 `ui-monospace` 排在最前，实际命中的兜底字体字形偏瘦。
- **行高 1.75 → 1.7**，字号变大后略微收紧，保持每屏可读行数。
- 编辑页输入框同步从 13.5px/400 提到 14.5px/500。

### 修复

- 设置页「启用右键菜单」改后不生效：service worker 监听的是 `changes.jsonFormatterSettings`，
  但设置项是**平铺**写进 storage 的（属性名就是 `contextMenu`），条件永远不成立。
  改为监听 `changes.contextMenu`。此前只有重启浏览器才会重建菜单。

### 修复 · 连续输入时的窗口闪动

编辑页刚做出来时，连续打字或反复粘贴会让输出区「一明一暗地闪」。排查下来是四处各自独立
触发、又被 220ms 防抖反复重放的动画叠加而成，逐一去掉：

- **输出面板的扫描光条**（`.panel-output.is-busy::before` + `@keyframes scanBar`）：
  面板每次进入「解析中」都会从头扫一遍，是闪动最明显的一处。直接删除该状态样式。
- **输出面板的状态环**（`.panel-output.is-flash` + `@keyframes panelFlash`）：
  用 `box-shadow` 向外扩散一圈，和上一条同时触发时闪感翻倍。一并删除。
- **行入场动效里的 `filter: blur()`**：`blur(2px) → blur(0)` 逐行过渡时会有一帧轻微抖动，
  看起来像重影。去掉模糊，只保留位移与透明度。
- **重播动画时先摘类再重加**：原先为了「让动画能重新触发」写成
  `classList.remove() → 强制重排 → classList.add()`，那一刻所有行会从 `opacity:1`
  掉回基态再跳到 `opacity:0`，观感就是整块内容闪一下。实际上每次格式化都会重建 `.jf-row`，
  新元素天然从关键帧起点开始播，改成**只加不清**即可。
- **状态胶囊圆点的循环脉冲**（`@keyframes pulse` infinite）：胶囊在「解析中…」与
  「格式化成功」之间来回切换，每切一次循环动画就重头播一遍。改成三档**静态**配色表达状态
  （灰=就绪 / 蓝=解析中 / 绿=成功 / 红=失败），不再有任何循环动画。

`tools/verify-editor.py` 增加了对应的回归检查：用 `MutationObserver` 盯住输出面板的 class，
确认连续输入时 `is-busy` / `is-flash` 从未出现，并直接读取胶囊圆点的 `animationName`
断言其为 `none`；同时断言全屏切换后 `html.is-fullscreen` / `.workspace.is-focus`
成对出现与收起、输入面板随之 `display:none` / 恢复。

### 新增 · 设置项

- 设置页新增「**使用等宽字体**」开关。关闭后正文改用系统正文字体，同字号下观感更大更清晰。
  此前 `monoFont` 配置项存在但未接入任何界面，属于死配置。

## 1.0.0 — 2026-09-20

首个版本。

### 新增

- **自动接管**：识别 `application/json`、`*/json`、`application/*+json` 响应后自动格式化；`text/plain` 返回的 JSON 可选自动格式化。
- **语法高亮**：键名加粗、字符串 / 数字 / 布尔 / null 分色；浅色与深色两套主题，可跟随系统。
- **可折叠树**：逐节点折叠展开、折叠/展开全部、按层级展开（1~8 层）、折叠摘要。
- **搜索定位**：实时匹配键名与值（含转义内容），命中计数、上下跳转、自动展开命中路径、标记高亮。
- **路径复制**：点击键名复制 JSONPath，点击值复制值。
- **保留转义**：在「保留源码转义写法」与「还原为真实字符」之间切换。
- **输出控制**：美化 / 压缩、缩进 2 / 4 空格 / Tab、复制、下载 `.json`。
- **精度保真**：自研解析器保留源码原始切片，超出安全整数范围的大整数不被截断。
- **错误诊断**：非法 JSON 给出行列号与插入符定位；提供宽松解析重试（注释、单引号、尾随逗号、裸键）。
- **多入口**：工具栏弹窗、右键菜单（页面 / 选中内容）、快捷键 `Alt+Shift+J`、独立设置页。
- **样式隔离**：查看器根节点 `all: initial` 阻断宿主页面样式继承，自带注入 `<style>` 定义全部配色与度量。

### 已知限制

- 仅处理顶层文档，不在 iframe 内运行。
- 单页应用（SPA）在路由切换后新加载的 JSON 不会自动重新接管，需手动点击弹窗或快捷键。
- 浏览器内置页面（`edge://`、`chrome://`、扩展商店页面）无法注入。
