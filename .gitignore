# 构建产物 —— 用 `node tools/build.js` 可随时重新生成，不入库
dist/

# 依赖与缓存
node_modules/
__pycache__/
*.pyc
*.pyo

# 临时与日志
.tmp_*
*.log

# 调试用 Edge 浏览器临时配置（browser-automation-cdp 启动时生成）
.edge-profile/

# 性能回归夹具（11MB 真实数据，只在本地用，不入库）
tools/fixtures/
